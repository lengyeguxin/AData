# AData主配置文件示例
# 请复制此文件为 config.yaml 并填入您的实际token

# 数据库配置
database:
  path: database/adata.db
  type: duckdb

# Tushare API配置
tushare:
  token: YOUR_TUSHARE_TOKEN_HERE  # 请替换为您的Tushare Token
  api_url: YOUR_TUSHARE_API_URL   # Tushare（代理）API地址
  rate_limit: 500  # 每分钟500次（1万积分）
  random_sleep_min: 1  # 随机休眠最小值（秒）
  random_sleep_max: 0  # 随机休眠最大值（秒），设置为0表示禁用休眠

# 数据拉取配置
fetch:
  enabled: true                    # 数据拉取开关（默认开启）
  start_date: "20210101"           # 最早开始时间（全局默认）
  check_interval: 60               # 检测间隔（分钟）
  max_retries: 2                   # 失败重试次数
  retry_delay: 10                  # 重试间隔（秒）
  timeout: 60                      # API超时（VIP需要60s）

# 快照配置
snapshot:
  enabled: true                    # 快照开关
  interval: 60                     # 快照间隔（分钟）
  locations:                       # 快照位置
    - database/adata_snapshot.db
    # - /path/to/your/snapshot.db  # 第二个快照位置（可选）

# 调度器配置
scheduler:
  enabled: true
  # 注意：weekly_update_time和monthly_update_time配置已弃用
  # 数据拉取使用check_interval机制（每60分钟检查一次）

# 日志配置
logging:
  level: INFO
  file: logs/adata-backend.log
  error_file: logs/adata-backend-error.log
  max_size: 10MB
  backup_count: 5